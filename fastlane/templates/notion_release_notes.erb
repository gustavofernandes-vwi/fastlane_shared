# Release notes <%=version.title.title%>

<%
tasks.each do |task|
    line_url = task.public_url || task.url
    line_id = ""
    line_id = "[#{task.unique_id}](#{line_url})"

    line_origin = ""
    origin = task.properties["Origem ID"].text
    line_origin = "(#{origin}) "

    line_title = task.title.to_s
-%>
<%=line_id%> <%=line_origin%><%=line_title%>

<% end %>